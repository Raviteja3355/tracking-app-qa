# Work Log — 2026-05-12

全天 commit 记录，按开发顺序整理。

---

## 1. 项目初始化 `d1e6f65`

- `next.config.ts`：配置 `output: 'export'`（纯静态）、`images.unoptimized: true`
- `app/globals.css`：全局样式、Tailwind 自定义 token（`uni-black`、`uni-red`、`brand` 等）、响应式断点、自定义滚动条
- `app/providers.tsx`：`QueryClientProvider`（react-query）根组件
- `app/layout.tsx`：初始根 layout（后续被 route group 替换）
- 依赖安装：react-query、react-i18next、i18next、xlsx、file-saver 等

---

## 2. 清理 Claude Code 配置 `8c32032`

- 删除 `AGENTS.md`、`CLAUDE.md`，不纳入版本管理

---

## 3. Layout 组件 `12ad12d`

- **`components/icons/Logo.tsx`**：UniUni SVG Logo 组件
- **`components/icons/ChevronDown.tsx`**：下拉箭头 SVG
- **`components/layout/Header.tsx`**：导航栏，语言切换下拉（EN / FR），sticky top，lang 状态从 URL 初始化
- **`components/layout/Footer.tsx`**：底部版权 + 链接
- **`components/layout/FAQ.tsx`**：手风琴 FAQ，动画展开/收起
- **`components/layout/CustomerSupport.tsx`**：联系支持表单，Freshdesk ticket API 提交，省市联动，文件附件上传，i18n
- **`components/layout/Intercom.tsx`**：Intercom widget 初始化（`useEffect` 注入，后续重构）

---

## 4. 核心 Lib — 类型、工具、API、Hooks `9a0bbe0`

- **`lib/types/index.ts`**：`TrackingResult`、`EddData`、`SpathItem`、`PodResult` 等完整类型定义
- **`lib/api/tracking.ts`**：tracking API 请求封装
- **`lib/api/edd.ts`**：预计送达日期（EDD）API
- **`lib/api/pod.ts`**：POD（送达证明）获取，支持 URP 直接获取和邮编验证两种路径
- **`lib/api/notice.ts`**：公告 API
- **`lib/api/config.ts`**：API base URL 配置（从 env var 读取）
- **`lib/hooks/useTracking.ts`**：批量查询 hook，支持最多 25 个单号，并发请求，错误聚合
- **`lib/hooks/usePod.ts`**：POD 获取 hook，含邮编验证流程
- **`lib/utils/formatTime.ts`**：时间格式化工具
- **`lib/utils/validation.ts`**：URP 单号格式验证
- **`lib/utils/watermark.ts`**：PDF 水印生成工具

---

## 5. TrackingApp 主组件 `93b0039`

- **`components/tracking/TrackingApp.tsx`**：主追踪流程控制，管理查询状态、结果、弹窗开关，处理 POD / ZipModal / PodModal 逻辑
- **`components/tracking/TrackingInput.tsx`**：单号输入框，支持多行（批量），清除按钮，提交
- **`components/tracking/Loader.tsx`**：加载动画组件

---

## 6. Tracking 结果 & 弹窗组件 `a84a4d3`

- **`components/tracking/results/ResultCard.tsx`**：单条结果卡片核心内容，含 EDD、Tracking Number（复制）、Label Created、Package Weight、4 步进度条、Help Strip、Tracking History 时间线，POD 按钮
- **`components/tracking/results/ParcelCard.tsx`**：结果卡片折叠容器，可展开/收起，支持 `collapsible` prop（单结果时固定展开，无箭头）
- **`components/tracking/results/TrackingResults.tsx`**：多结果列表，单结果时 `collapsible=false`
- **`components/tracking/results/EddBlock.tsx`**：预计送达日期展示块
- **`components/tracking/results/TimelineItem.tsx`**：时间线单条事件，含 POD 查看按钮（state 203）
- **`components/tracking/results/ExportTable.tsx`**：Excel 导出表格数据组装
- **`components/tracking/modals/ZipModal.tsx`**：邮编输入弹窗，提交后获取 POD，z-index 修复为 `z-9999`
- **`components/tracking/modals/PodModal.tsx`**：POD 图片展示弹窗，支持下载
- **`components/tracking/modals/NoticeModal.tsx`**：公告弹窗

---

## 7. i18n 双语支持 `6a3d5a3`

- **`lib/i18n/index.ts`**：i18next 初始化，locale 从 `window.location.pathname` 检测（`/fr/` 前缀 → FR，其余 → EN）
- **`lib/i18n/locales/en.json`** / **`fr.json`**：约 90 个翻译 key，覆盖所有 UI 文案
- 新建 `app/tracking/page.tsx`（EN 路由）、`app/fr/suivi/page.tsx`（FR 路由）

---

## 8. 文档归档 `9b44e93`

- `docs/PRD.md`：产品需求文档
- `docs/ERD.md`：原始工程设计文档
- `docs/ERD_Revision.md`：修订版 ERD（10 节 + 5 个 ADR + 风险登记）
- `docs/static_page.html`：UI 参考静态原型
- `docs/shortcoder/`：线上 WordPress shortcoder 源码（tracking form EN/FR、customer support form EN/FR、chatbot 等）

---

## 9. 修复杂项 + AlertIcon `55e61c3`

- **`components/tracking/icons/AlertIcon.tsx`**：红色警告圆圈 SVG，用于无效单号提示
- `.gitignore` 更新

---

## 10. CustomerSupport 对比分析 `a91c717`

- **`docs/KNOWN_ISSUES.md`** 新建：对比 shortcoder 源码，记录 5 项设计偏差：
  1. Contact Reason 选项与 Freshdesk ticket type 不匹配
  2. Incident 子选项静态写死，未按 reason 动态联动
  3. `custom_fields` CF 字段映射错误
  4. Priority 逻辑过于简化（应由 reason + incident 组合决定）
  5. 电商商家字段（SHEIN / TEMU / TikTok）未实现

---

## 11. SEO 100% 复原 `2381a7e`

- **删除 `app/layout.tsx`**，改为 route group 双 layout：
  - `app/(en)/layout.tsx`：`lang="en-CA"`，完整 EN metadata（title、description、keywords、canonical、hreflang、og:*、twitter:*、favicon CDN、msapplication-TileImage、themeColor via Viewport）
  - `app/(fr)/layout.tsx`：`lang="fr-CA"`，完整 FR metadata
- 页面文件迁移至对应 route group：`app/(en)/page.tsx`、`app/(en)/tracking/page.tsx`、`app/(fr)/fr/suivi/page.tsx`
- `CustomerSupport.tsx`：Contact Support 按钮加 hover 效果（`hover:bg-[#2a2a2a] hover:-translate-y-px`），对标静态 HTML
- `ParcelCard.tsx`：新增 `collapsible` prop，单结果时固定展开、无箭头
- `TrackingResults.tsx`：单结果时传 `collapsible={false}`
- `docs/SEO_AUDIT.md` 初版：EN/FR SEO tag 对照表

---

## 12. SEO 文档完善 `9e86817`

- `docs/SEO_AUDIT.md` 全面更新：EN/FR 所有 tag 逐项对比（✅ / ⚠️），实现说明，验证方法（curl diff、AITDK、社交预览工具、GSC）

---

## 13. Keywords / Favicon / robots.txt / sitemap.xml `806ce98`

- `app/(en)/layout.tsx` + `app/(fr)/layout.tsx`：补充 `keywords` meta，补齐 favicon CDN URL（32x32、192x192、180x180、270x270）
- `public/robots.txt`：`User-agent: * / Allow: / / Sitemap: https://www.uniuni.com/sitemap.xml`
- `public/sitemap.xml`：EN + FR 两条 URL，含 xhtml:link hreflang 互指

---

## 14. Accessibility 修复 `f183afa`

- **`<main>` landmark**：EN / FR `page.tsx` 包进 `<main>`，修复 Lighthouse "Document does not have a main landmark"
- **Form label 关联**：`FieldLabel` 从 `<div>/<span>` 改为 `<label htmlFor>`，所有表单控件加对应 `id`，修复 "Select elements do not have associated label elements"
- **Header 链接优化**（AITDK Links 审查）：
  - Logo 加 `title="UniUni"` + `<span className="sr-only">UniUni</span>`（修复 Anchor Missing）
  - FAQs / Support 加 `title={t(...)}`，随语言切换
  - 语言切换下拉加 `title={lang.label}`

---

## 15. Analytics 集成 + Intercom 重构 + Stepper 修复 `5923b44`

- **`components/layout/Analytics.tsx`** 新建：
  - GTM snippet（`strategy="afterInteractive"`）+ noscript fallback
  - HubSpot script（`strategy="lazyOnload"`）
  - env var 控制，未配置时静默跳过
- **`components/layout/Intercom.tsx`** 重构：`useEffect` + 手动 DOM 注入 → `next/script lazyOnload`，44 行 → 24 行，去掉 `'use client'`
- `.env.local`：新增 `NEXT_PUBLIC_GTM_ID=GTM-563V498`、`NEXT_PUBLIC_HUBSPOT_ID=49003739`
- 两个 layout 引入 `<Analytics />`
- **Stepper bar 修复**：连接线 `left/right` 从 `12.5%` 改为 `calc(12.5% + 22.5px)`，补偿 grid `padding: 0 30px` 导致 bar 两端超出圆心的偏移

---

## 16. 文档 `5923b44`

- **`docs/ANALYTICS_AUDIT.md`**：线上埋点清单（10 项）、PRD 要求 vs 当前状态、GTM jQuery 问题根因与解决方案、各 tag 代码示例、验证步骤
- **`docs/PENDING_BLOCKERS.md`**：所有待解决问题汇总，分 P0 Blocker / P1 Important / Design 缺口 / 已推迟四档

---

## 遗留 / 待跟进

| 项 | Owner | 状态 |
|---|---|---|
| GTM 容器清理 WordPress jQuery tag | Marketing | 待操作 |
| Intercom App ID 填入生产环境变量 | DevOps | 待操作 |
| CustomerSupport 表单 Freshdesk 映射修复 | Product / Engineering | 待决策（Option A/B） |
| 设计稿补充：Error toast、状态码→4步骤 mapping、批量导出 | Design | 待输出 |
| 后端 API 增加 package weight 字段 | Backend | 待确认 |
| URL deep link `?no=` 参数支持（PRD F-06） | Engineering | 待实现 |
| Analytics 上线后验证（Tag Assistant / Pixel Helper / Clarity） | Marketing / Engineering | 部署后执行 |
| react-query hydration warning（`__ssr__`）| Engineering | 已推迟 |
