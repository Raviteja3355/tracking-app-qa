# Work Log — 2026-05-12

---

## Accessibility（Lighthouse Accessibility 89 → 修复 2 项）

- **`<main>` landmark**：EN / FR 两个 `page.tsx` 的页面内容包进 `<main>` 元素，修复 "Document does not have a main landmark"
- **Form label 关联**：`FieldLabel` 组件从 `<div>/<span>` 改为 `<label htmlFor>`，所有 `<select>` / `<input>` / `<textarea>` 加对应 `id`，修复 "Select elements do not have associated label elements"
- 对比度问题确认不改（品牌色设计决策）

## Header 链接优化（AITDK Links 审查）

- Logo 链接加 `title="UniUni"` + `<span className="sr-only">UniUni</span>`，解决 Anchor Missing
- FAQs / Support 导航链接加 `title={t('navFaqs')}` / `title={t('navSupport')}`
- 语言切换下拉链接加 `title={lang.label}`

## Stepper 连接线 Bug 修复

- 连接线 `left/right` 从 `12.5%` 改为 `calc(12.5% + 22.5px)`，补偿 grid `padding: 0 30px` 导致 bar 超出圆心的偏移

## Analytics 集成（Placeholder）

- **`components/layout/Analytics.tsx`** 新建：GTM snippet（`afterInteractive`）+ HubSpot script（`lazyOnload`），env var 控制，未配置时静默跳过
- **`components/layout/Intercom.tsx`** 重构：`useEffect` + 手动 DOM 注入 → `next/script`（`lazyOnload`），去掉 `'use client'`，代码从 44 行缩到 24 行
- `.env.local` 新增 `NEXT_PUBLIC_GTM_ID` / `NEXT_PUBLIC_HUBSPOT_ID` placeholder
- 两个 layout 引入 `<Analytics />`

## 文档

- **`docs/ANALYTICS_AUDIT.md`** 新建：线上埋点清单、PRD 要求 vs 实现状态、GTM jQuery 问题分析、GTM 清理方案、各 tag 实现代码片段、验证方法
- **`docs/PENDING_BLOCKERS.md`** 新建：汇总所有待解决问题，分 P0 Blocker / P1 Important / Design 缺口 / 已推迟四档

---

## 遗留 / 待跟进

| 项 | Owner | 状态 |
|---|---|---|
| GTM 容器清理 WordPress tag | Marketing | 待操作 |
| Intercom App ID 填入生产环境变量 | DevOps | 待操作 |
| CustomerSupport 表单 Freshdesk 映射修复 | Product / Engineering | 待决策 |
| 设计稿补充：Error toast、状态码→步骤 mapping、批量导出 | Design | 待输出 |
| 后端 API 增加 package weight 字段 | Backend | 待确认 |
| Analytics 上线后验证（Tag Assistant / Pixel Helper / Clarity） | Marketing / Engineering | 部署后 |
